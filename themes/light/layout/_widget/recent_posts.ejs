<% if (is_post() && site.posts.length){ %>
<div class="widget tag">
  <h3 class="title">最新文章</h3>
  <ul class="entry">
    <% site.posts.sort('date', -1).limit(5).each(function(post){ %>
      <li>
        <a href="<%= config.root %><%= post.path %>"><%= post.title %></a>
      </li>
    <% }); %>
  </ul>
</div>
<% } %>
